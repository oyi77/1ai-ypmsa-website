{
  "name": "1ai-ypmsa-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -fR public/* dist/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@astrojs/tailwind": "6.0.2",
    "@fontsource/inter": "^5.2.8",
    "@fontsource/plus-jakarta-sans": "^5.2.8",
    "astro-icon": "^1.1.5"
  }
}
